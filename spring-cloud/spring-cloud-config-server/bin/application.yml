spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kelvinikome/ConfigData
          searchPaths: ConfigData
server:
  port: 8001
          